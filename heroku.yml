setup:
  addons:
    - plan: jawsdb-mysql
      as: JAWSDB

build:
  docker:
    ghost: Dockerfile

release:
  image: ghost
  command:
    - node create-config.js

run:
  ghost: node current/index.js
